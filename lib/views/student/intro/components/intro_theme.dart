import 'package:flutter/material.dart';

/// Shared colors/spacing for the intro flow.
class IntroTheme {
  static const Color blue = Color(0xFF0055FF);
  static const Color text = Color(0xFF0F172A); // slate-900
  static const Color subText = Color(0xFF334155); // slate-700
  static const double hPad = 24;
  static Color lightGray = const Color.fromARGB(
    255,
    103,
    108,
    119,
  ); // slate-200
}
